<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>ForFun IaC Basic</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style> 
    body{
      font-family: system-ui;
      margin: 3rem;
      line-height: 1.6;
    } 
    code{
      background: #f2f2f2;
      padding: 2px 6px;
      border-radius: 6px;
    }
    .info{
      background: #f8f9fa;
      padding: 1rem;
      border-radius: 8px;
      margin: 1rem 0;
    }
    .success{
      color: #28a745;
    }
    .meta{
      color: #6c757d;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <h1>🚀 Déploiement réussi !</h1>
  <p>Instance gérée par <strong>Terraform</strong> et configurée par <strong>Ansible</strong>.</p>
  
  <div class="info">
    <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
    <p><strong>Public IP:</strong> {{ hostvars[inventory_hostname]['ansible_host'] | default(inventory_hostname) }}</p>
    <p><strong>Application:</strong> ForFun IaC Basic v1.0.0</p>
    <p><strong>Déployé le:</strong> {{ ansible_date_time.iso8601 }}</p>
  </div>
  
  <div class="meta">
    <p>🚀 <strong>Terraform</strong> + <strong>Ansible</strong> = Infrastructure as Code</p>
    <p>Instance configurée automatiquement avec succès !</p>
  </div>
  
  <hr>
</body>
</html>
